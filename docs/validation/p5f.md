# P5F validation — PASS under V1.0 Amendment 002

## Final P5F disposition (2026-09-27)

The project owner formally moved the two-real-human same-instance trial from the original P5F gate to A.7 in [V1.0 Amendment 002](../specs/v1-amendment-002-human-trial-gate.md). The owner accepted the current player and admin UI as P5-final. Under the amended **development** standard, P5F is **PASS** on the following existing evidence; no new instance was created merely to repeat it:

- One real owner completed the formal Player Web Flow for Workshop `2307479570/custom`: Web request `230f2d1a-7bc8-4f7d-8026-7c9d6e15d3a6` → Allocation → d2core create → Ready/valid JoinInfo → owner used the page-provided connect command, entered and played → page stop → `ended/reclaimed`. The recorded request-to-JoinInfo interval was 24.0 seconds. The later single-person Player Web flow after Amendment 001 also reached real entry, play, stop and full reclaim. These are one-human trials, not multiplayer trials.
- Two independent anonymous sessions completed create Party, invite retrieval/reset, stale-token rejection, join, both seeing the same two-member Party, member leave, leader disband and logout through the development player API; the owner separately confirmed normal behavior in two browser windows. [P2D](p2d.md) and PostgreSQL integration tests cover leader/member permissions and forged identity/foreign-Party rejection. Two sessions and two windows are **not** two real people or two Dota clients.

**REAL MULTI-HUMAN SAME-INSTANCE TRIAL: NOT VERIFIED — DEFERRED TO A.7. BLOCKS FINAL V1 RELEASE.** Two or more real people must still join the same Party and real Dota instance through the formal Player Flow, play, end or start the next game, and reach full reclaim during small production rollout. No Bot, mock, duplicate browser session or single-person multi-client result is accepted as that evidence.

## Historical record under the original P5F gate

No P5F multiplayer result is claimed. The required trial needs at least two real people to join the same real Dota instance through the player flow, observe normal lifecycle and full reclaim, and record natural request/assignment/create/Ready/JoinInfo timestamps. The owner has now confirmed one real person entering a maintenance instance and normal gameplay; this was outside the player flow and does not substitute for the two-person P5F trial. Bots, browser sessions and unit tests do not substitute for this trial.

Owner-provided P4 carry-over hypothesis: the template's Steam login success Ready marker can vary after other startup conditions are already satisfied, and may explain earlier roughly 55-second `create_started_at → ready_at` samples. P5F should correlate existing create, process/startup, other Ready markers, Steam login success, d2core Ready and Platform `ready_at` evidence where logs permit. Preserve the Steam login condition and JoinInfo gate; do not infer scheduler regression from that wait. Controller's roughly five-second claim polling is a separate future optimization candidate. No d2core protocol or large telemetry/performance change is authorized by this observation alone.

During P5A/P5B template validation, a drained Linux `dota` mode instance offered a concrete diagnostic example: an intermediate official d2core status showed the process running and the loading/host-activate/manual-setup markers matched, while the Steam login success marker was absent and the room remained `loading`. A later status showed the Steam login marker and `ready`; the instance was then fully reclaimed. This was a direct maintenance instance, not a Platform Allocation, so it provides no Platform scheduler latency sample or player time-to-join measurement. Correlate its preserved d2core operation/engine logs with future P5F Platform samples rather than treating the intermediate state as an error.

After the owner's one-person maintenance test, a separate normal player API request for the same content recorded `requested_at` 12:20:20.066 UTC, `assigned_at` 12:20:21.925, `create_started_at` 12:20:24.432, `ready_at` and `join_info_available_at` 12:20:44.371, and `reclaimed_at` 12:20:54.378. Thus request-to-JoinInfo was 24.3 seconds, with create-started-to-Ready 19.9 seconds. This request reached valid JoinInfo and full reclaim but had no human player join; keep it separate from multiplayer evidence. Existing d2core log correlation appears below; Ready and telemetry were unchanged.

The owner also entered separate `dota` and `hard` maintenance instances and confirmed normal gameplay in each. Subsequent normal player API probes, again without a human join, recorded request-to-JoinInfo of 35.0 and 45.5 seconds and create-started-to-Ready of 29.9 and 39.9 seconds, respectively. An old-game `n6` probe took 30.9 seconds request-to-JoinInfo and 24.9 seconds create-started-to-Ready. All three reached valid JoinInfo and full reclaim. These few samples are evidence for later log correlation, not a performance distribution or proof that Steam login caused any particular interval.

The same owner later joined the old game's legacy content on Linux and the local Windows VM in separate maintenance sessions; both reached full Ready and were fully reclaimed. These confirm content and local Windows connectivity, but still involve one person and maintenance instances. The two-person P5F player-flow trial remains pending.

## Existing-log startup correlation

Three fully reclaimed player API probes can be tied by `NodeJob.instance_id` to their preserved d2core engine logs. The engine lines have one-second timestamps, so their boundaries are approximate; Platform timestamps below have database precision. The Windows VM uses China Standard Time, and its engine times have been converted to UTC in this table. The logs show a Steam login marker after the other visible startup markers, then Platform Ready/JoinInfo. They do not expose a separate precise d2core Ready timestamp, so this is evidence of the order and a possible contributor to variation, not a full per-stage latency breakdown.

| Content / instance | Platform `create_started_at` | Engine map load | Engine active / manual setup / host activation | Engine Steam login success | Platform `ready_at` = `join_info_available_at` |
| --- | --- | --- | --- | --- | --- |
| new custom, `i_c87ac4d7e56ac6e37f852ba0f0ace081` | 12:20:24.432 UTC | 12:20:30 | 12:20:34–35 | 12:20:41 | 12:20:44.371 UTC |
| new hard, `i_15a4dabd6814d0d7345597713d3de3df` | 12:38:14.402 UTC | 12:38:22 | 12:38:26 | 12:38:50 | 12:38:54.347 UTC |
| Windows old n6, `i_917451fb9ac57ada2a40fd726e6138ae` | 07:41:10.868 UTC | 07:41:15 | 07:41:17–18 | 07:42:02 | 07:42:05.787 UTC |

In the Windows sample, the other listed markers preceded the successful Steam login by roughly 44–45 seconds, and `create_started_at → ready_at` was 54.9 seconds. The comparable intervals were roughly 24 seconds in the Linux hard sample and 6–7 seconds in the Linux custom sample. These sequences are consistent with the owner's Steam-login-wait hypothesis, including one of the earlier approximately 55-second observations. They do not prove that Steam login accounts for every remaining second or that Platform scheduling regressed. The Steam-login Ready rule and JoinInfo gate were unchanged, and no telemetry/protocol or performance refactor was introduced.

Two isolated anonymous sessions were also exercised against the live development player API without creating a server: create Party, fetch and reset invite, reject the stale token, join with the new token, observe the same two-member Party from both sessions, member leave, leader disband, and logout. Every step passed, and both sessions were cleaned up. This validates the Web-facing Party state flow but is not a two-person Dota join. The owner confirmed only one person and one Dota client are currently available; the required two-person trial is still **NOT VERIFIED**.

The owner then exercised the same Party flow through two real browser windows and reported normal results. In a separate single-person player Web trial, Workshop `2307479570` `custom` on Linux used ServerRequest `230f2d1a-7bc8-4f7d-8026-7c9d6e15d3a6`: `requested_at` 14:25:47.072 UTC, `assigned_at` 14:25:48.974, `create_started_at` 14:25:50.385, `ready_at` = `join_info_available_at` 14:26:11.031, and `reclaimed_at` 14:27:15.328. Request-to-JoinInfo was 24.0 seconds. The owner reported normal Ready, page-provided `connect` entry, gameplay and page stop; database state reached `ended/reclaimed`. The owner also reported the player elapsed display stepping backwards at waiting-to-creating and creating-to-Ready transitions. The UI timer correction is documented in `p5-ui-parity.md` and needs a live recheck. This remains one human; no two-person P5F acceptance is claimed.

After P5C Amendment 001 opened Linux Steam, the owner performed another single-person player flow and reported the flow normal. The owner deliberately toggled Steam entry closed and open in Admin to inspect both player states, then highlighted entry UI hierarchy issues. The temporary instance fully reclaimed and no Allocation, open Job or active Request remained. The follow-up UI correction is tracked in `p5-ui-parity.md`. The owner still has one person and one Dota client available, so the required two-real-person same-instance trial remains **NOT VERIFIED**.
