# P2 Party UI focused parity checkpoint

This is a point-in-time parity checkpoint. The later [P2 final validation](p2-summary.md) records the project owner's confirmed UI acceptance and overall P2 PASS.

Date: 2026-09-25. Parent functional checkpoint: `69450e1eff23a85e3e9dd203b1c8ba4d7a7c4b74`.

The formal Party page was compared against `prototype/p1/party.html` and `party.css` while keeping the accepted P1 server panels. It has the same two-column desktop/card hierarchy and one-column mobile flow: member rows and leader/member badges in the main card, a separate invitation card with copy/reset emphasis, and a current-server summary card at the side. The formal page uses existing P1 design colors, typography, buttons and spacing rather than mock API/data. Future P3/P4 prototype controls remain absent.

Final inspection found that a query-string invitation URL could propagate its credential through page requests or Referer headers. The formal link now uses `#invite=...`; fragments are not sent as the HTTP request target. The landing page reads this fragment, the join API receives only the token body, and the browser URL is cleaned after a successful join. A frontend test asserts the generated URL has an empty query and the expected fragment. Invite token values are not recorded here.

Web lint, typecheck, tests (including the new invite-link test), and build: PASS. The in-app browser tool timed out, so a separate headless Edge run used three isolated browser contexts and formal server-issued Sessions. A created a Party and copied the fragment link; B joined; A/B observed the same members and roles. Invite reset rejected the old link, the new link admitted C, B left, A removed C, and A formally disbanded the idle test Party. Desktop and 390px mobile screenshots were inspected with the sensitive invite field masked; no layout break was found. A same-tab switch from an old invite fragment to a new one revealed that the page initially read the fragment only at mount; follow-up checkpoint `e088c2e89b4d23b503281b7a5c1e9abed4f54fe1` now listens for `hashchange`, and the full Edge sequence passed afterward. The owner has been asked for one final UI confirmation before P2 overall PASS is declared.

Independent parity checkpoint `4bc23c25c70837a1d9e856fad624b83856358ff4` was pushed to `origin/main`; GitHub CI run `36134798500` completed SUCCESS. The follow-up UI source checkpoint is `e088c2e89b4d23b503281b7a5c1e9abed4f54fe1`; GitHub CI run `36135703388` completed SUCCESS. Its exact Web bundle (`index-BhiryNsP.js`, `index-DklfucEu.css`) is served from `/srv/arcade-dev/web/platform/web-dist-p2d-parity-e088c2e`. HTTPS homepage, both hashed assets, and health returned 200. The Platform executable remains the clean P2D functional build; no Node, d2core, or game content changed.

The first Edge attempt stopped after A/B join while exercising the old-to-new same-tab link and left one additional idle test Party with two members; its ephemeral browser Sessions were destroyed. This retained Party is identified in the ignored environment inventory and total report. It was not deleted directly from PostgreSQL. The successful rerun used formal leave/remove/disband and left no additional live Party.
