# P3 final player UI parity checkpoint

The P3 Web source was compared with the accepted P-1 node-selection prototype and the owner-accepted P2 player/Party UI. It retains the established colors, type, cards, spacing and page hierarchy while adding auto/manual selection, presentation-safe Node names and status, waiting reason, assigned Node, and safe cancel. It does not expose Node credentials, d2core paths, data-dir, templates, SSH information, or private mapping configuration. JoinInfo is displayed only after the Ready and valid-entry conditions.

The clean P3F-source Web build was packaged and SHA256-checked before deployment to the existing **development** Platform. The exact bundle tar SHA256 was `8ba3a1a7156a0c6b37f8d657ac443e523e1a07c5fa7057a1b0a0db2e9a4cdcd9`; the new root is `web-dist-p3-ui-eac5551`, with JS `index-DfB6XrZ6.js` and CSS `index-HF-fJzOj.css`. The prior P2 Web root was preserved, and the launcher was backed up before its one-line Web-root change. The development Platform restarted briefly on the same tested P3 backend binary. Loopback and trusted HTTPS health, homepage, and both exact assets returned 200. No production, Caddy, systemd, firewall, NAT, or content configuration changed.

Isolated Edge contexts loaded the actual HTTPS development site at 1440, 390, and 320 CSS px. Each rendered the correct hashed asset, auto choice, two real Node choices and availability, and the preserved Party tab with no horizontal overflow. Desktop and mobile screenshots were visually compared to the P-1/P2 visual direction. A real manual choice of the temporarily Drained Windows development Node showed its maintenance state; submission persisted a manual waiting ServerRequest with the specified Node, displayed the correct waiting reason and safe cancel control, and created no Allocation. The cancel action changed that request to `cancelled`; the Node was Resumed immediately afterward. A separate isolated browser Session created a one-member Party, verified leader/member/invite/current-server presentation at desktop and mobile widths, masked the invite in screenshots, then formally disbanded only that test Party. Existing Parties were untouched.

Final read-only development state after the UI run: two cancelled and 22 ended ServerRequests, 22 reclaimed Allocations, no active Allocation, both Nodes Drain false with desired/hard 1/1, three pre-existing active Parties, empty d2core lists on Linux and Windows, and healthy Platform. The temporary deployment script was removed. Windows public JoinInfo and unavailable domain/NAT/asymmetric topologies remain **NOT VERIFIED**. Functional P3A–P3F and this self-parity check are complete; project-owner final UI confirmation is still required before P3 overall PASS and the total exit report.
