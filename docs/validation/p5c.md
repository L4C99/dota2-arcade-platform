# P5C entry checkpoint and Amendment 001

## Amendment 001 final convergence (2026-09-27)

The project owner further clarified the existing Amendment 001 after reviewing the Admin UI. This remains Amendment 001, not a new release candidate. The historical implementation and observations below are retained in their original context. The final contract separates Node `a2s_enabled`, per-Ready-instance administrator A2S diagnostics, revision-bound human `verified`, and routine `enabled`. The previous Node aggregate `a2s_query_ok` is deprecated/derived for compatibility. Admin Entry no longer displays query status; Allocation details show instance ID, local port, independent `ok/failed` result and check time, with absence meaning no query fact. Player API, JoinInfo and player UI have no A2S diagnostics.

Within one entry revision, human verification is a one-way confirmation: normal Admin `verified=false` is rejected and the revoke button is removed. Operational closure uses `enabled=false`, preserving the human record. An actual network revision change automatically clears both schemes. Forward migration 18 stores the current per-instance snapshot; migrations 15 and 17 remain unchanged. Existing Linux Steam URI evidence and the verified/enabled deployment fact must be preserved if its revision remains unchanged. Steamchina and Windows public entry remain unverified. This convergence does not require testing the rest of the port pool.

The final specification commit is `3c35f67`; implementation and migration are `660cb88`, followed by migration-test fixture correction `16d82b5` and heartbeat validation hardening `588acd3`. [Final CI run 36259208501](https://github.com/L4C99/dota2-arcade-platform/actions/runs/36259208501) passed Ubuntu, Windows, Web and PostgreSQL integration jobs. The preceding CI run found a test fixture still expecting migration 17 as the latest version; the forward migration itself and other jobs were sound. Local gofmt, `go test ./...`, `go vet ./...`, Web lint/typecheck, 10 Vitest tests and production build passed. PostgreSQL tests covered the disposable upgrade, independent per-instance facts, idle cleanup, Admin Allocation association, monotonic verification and URI independence.

Development deployment used a protected database backup, restored it into a disposable database, ran migration 17→18 there, and compared catalog/request/allocation/audit counts and both schemes' entry flags before touching the live development database. The first drill attempts stopped on a helper-script SQL typo and nonexecutable staged binary; the corrected drill passed without migrating the live database. The actual development migration and Platform/Web replacement then passed health checks. Both Linux and Windows development Controllers reported the new version and compatible fresh heartbeats. The unchanged Linux entry revision is `5ba121bd04a80c74813363bcf5e452ff5a2c7065f3076b560c789afe9453bd8c`; Steam remained verified and enabled, steamchina remained unverified and disabled. Both Windows public schemes remained unverified and disabled. No d2core, Dota, content, firewall, NAT or router configuration changed.

One temporary official `2307479570/custom` development Allocation reached Ready. Player JoinInfo exposed both `connect` and the already enabled Steam URI. The live administrator diagnostic associated that actual instance with local port 28000, `ok`, and a fresh check time. The instance then reached full reclaim; the idle diagnostic snapshot cleared and the Linux Steam attestation remained unchanged. Development Web `/admin` and its hashed asset returned HTTP 200. A temporary Admin browser session at 1440, 390 and 320 pixels confirmed no horizontal overflow; the Entry card had no verification-revoke action or A2S query result, while the Node section showed A2S deployment configuration. The temporary Admin account was disabled. This live diagnostic check did not repeat human URI entry; existing owner tests on 28000–28002 remain the human evidence. Steamchina URI and Windows public URI remain **NOT VERIFIED**.

Final API layering follow-up `b7b6930` moved `a2sEnabled` from the Admin Entry DTO onto the Admin Node DTO and removed the unused Entry port list. PostgreSQL integration asserts that Node contains the reported deployment fact while Entry contains no A2S or port-coverage field. [CI run 36260125876](https://github.com/L4C99/dota2-arcade-platform/actions/runs/36260125876) passed all four jobs. The code-only development Platform/Web update retained the prior binary, assets and launcher; schema stayed at migration 18, zero live resources were present, and entry flags/revisions were unchanged. A second temporary Admin browser session confirmed the same entry state, no revoke action, Node A2S configuration, and no mobile overflow. Both temporary Admin accounts were disabled.

During a later owner UI preview on 2026-09-27, the Linux development Node's steamchina entry was manually confirmed and briefly opened so both player buttons could be inspected. At the subsequent readback it was `verified=true`, `enabled=false` on unchanged revision `5ba121bd04a80c74813363bcf5e452ff5a2c7065f3076b560c789afe9453bd8c`; Steam remained verified and enabled, Windows remained unverified and disabled. No successful real steamchina-client URI entry was reported in this validation record, so the administrative flag is not cited here as a completed human entry test. Keep steamchina disabled until that test is performed and recorded. The Web-only UI deployment did not change these states.

## Amendment 001 supersession (2026-09-27)

P5C began under the frozen V1 full-port rule and implemented exact port-list evidence, a required note and complete coverage checks. During real operation the project owner found this model unmaintainable and formally replaced it with [V1.0 Amendment 001](../specs/v1-amendment-001-entry-verification.md). The historical observations below remain accurate for the rule and deployed code at that time; their old **partial evidence only** conclusion is superseded. Forward migration 17 removes the old coverage constraint and columns without editing migration 15. The new Admin API records an explicit, audited real-client confirmation for each scheme on the current Node entry revision, independent of A2S. No per-port evidence or note is required.

The Linux development node's Steam URI succeeded with a real client on 28000, 28001 and 28002; port 28003 had a successful A2S diagnostic without a human URI trial. Every temporary instance was fully reclaimed. The same documented `protocol_ip=58.216.8.72`, identity mapping 28000–28009 and A2S-enabled network facts still produce entry revision `5ba121bd04a80c74813363bcf5e452ff5a2c7065f3076b560c789afe9453bd8c`. This evidence qualifies Linux Steam for administrator verification and separate enablement. Steamchina and Windows public URI remain unverified; A2S success alone does not verify either.

The independent amendment spec commit is `e22c80d`; implementation is `3903b08`, followed by PostgreSQL fixture correction `106e061`. [CI run 36256025402](https://github.com/L4C99/dota2-arcade-platform/actions/runs/36256025402) passed Ubuntu, Windows, Web and PostgreSQL integration jobs. Local gofmt, `go test ./...`, `go vet ./...`, Web lint, typecheck, 10 Vitest tests and production build passed. The PostgreSQL run covered a real migration 16→17 upgrade preserving prior verified/enabled/time/actor state and dropping obsolete columns, along with the new confirmation, revocation, A2S independence and JoinInfo tests.

On the development Web host, a protected pre-upgrade PostgreSQL dump was restored into a disposable database and checked against the source counts before applying migration 17. The previous binary, Web bundle and launcher were retained. The new Platform and Web passed local health and HTTPS asset checks; active Allocation, Job and Request counts were 0 before and after deployment. Under the unchanged Linux revision above, two audited Admin actions recorded Steam `verified=true` then `enabled=true`, with `verified_at` and `verified_by`; the temporary Admin account was disabled afterward. Subsequent Controller heartbeats retained both flags while the idle `a2s_query_ok=false`. Linux steamchina and both Windows schemes remain `verified=false`, `enabled=false`. No further instances were started solely to cover the port pool.

## Historical implementation before Amendment 001

- Controller checks A2S_INFO over local UDP for every currently Ready instance, including challenge handling. A2S remains a diagnostic and does not determine `connect` or scheduling eligibility.
- Admin entry verification requires an explicit human confirmation, note and exact full set of currently configured public ports. Verified and enabled remain separate. A network revision change clears verification, enabled state and port evidence. Migration invalidates legacy verifications that lacked full-port evidence.
- Player page displays a Steam or steamchina URI only when the backend supplies it after verified and enabled checks. It always retains `connect` help, including the client cold-start and Windows Win+R fallback.
- Unit tests exercise A2S challenge. PostgreSQL tests cover partial-port rejection, full-port acceptance and revision invalidation. GitHub Actions [run 36231578148](https://github.com/L4C99/dota2-arcade-platform/actions/runs/36231578148) passed all four jobs.
- On the drained Linux development node, the operator backed up the Controller config, set `protocol_ip=58.216.8.72` and enabled A2S diagnostics without changing the ten-port identity mapping. The Controller reported a fresh network revision and both URI entries remained unverified and disabled. An official temporary old-game instance reached full d2core Ready on 28000; the Controller reported `a2s_query_ok=false`, and direct local UDP A2S_INFO queries to loopback and the public IP timed out. The instance was stopped and fully reclaimed, the node reconciled and resumed. This is a failed diagnostic observation, not evidence that `connect` or either URI fails.
- The Admin verification action and button no longer require A2S query success. They still require explicit human confirmation, a note and every configured public port. This restores the frozen spec's separation between A2S diagnostics and real-client URI evidence. GitHub Actions [run 36244759743](https://github.com/L4C99/dota2-arcade-platform/actions/runs/36244759743) passed all jobs. Exact clean Platform/Web build `804109f` is live on the development Web host; HTTPS health and hashed Web asset returned 200. Both development nodes' verification and enabled flags remain false.

## Historical NOT VERIFIED status under the old rule

- Real client entry for Steam and steamchina across every configured public port, browser cold-start and asymmetric NAT behavior. No entry has been marked verified on development nodes. P5C is not marked PASS.
- The current mapping sets are Linux 28000–28009 and Windows 28100–28109. The earlier real-client `connect` checks on Linux 28000 and Windows 28100 do not establish URI behavior on either complete set. Windows still has no independently verified public URI route.

## First owner Steam URI attempt on Linux port 28000

After the owner authorized a Steam-first URI trial, the idle Linux development node was drained and a single direct fixed-d2core instance of the verified new-game `custom` template was created explicitly on port 28000. The instance reached `active/running/ready`, including Steam login success. The owner attempted `steam://connect/58.216.8.72:28000` and reported that Steam failed to start Dota; exact client error and ordinary `connect` result were not yet supplied. This is a failed URI attempt, not a successful port verification.

Read-only diagnosis found that the Controller network config declares `a2sEnabled=true`, but UDP A2S_INFO queries to both loopback and the public IP on port 28000 timed out while the instance was Ready. The current Dota `gameinfo.gi` had no `GMS` or `Advertise` stanza; fixed d2core's A2S documentation calls for `GameInfo/GMS/Advertise 1` and notes that a new instance plus a separate query are required after changing it. The A2S gap may contribute to the URI failure, but the owner's client symptom is not yet specific enough to prove causation. No Dota installation file, firewall or network configuration was changed during this diagnosis.

At the owner's request, the exact temporary instance was stopped and confirmed `lifecycle=reclaimed`, `process=stopped`, `cleanup=complete`; d2core's active list was empty. The Controller's explicit reconcile completed generation 11/11, and the Linux node was resumed. Both URI verification/enabled flags remain false. Further port trials are paused pending owner authorization for the proposed reversible development Dota A2S configuration change and a fresh real-client check.

## Authorized Linux development A2S correction and partial Steam URI trial

The owner explicitly authorized changing the **Linux development** Dota installation using fixed d2core v0.1.1 `a2s enable`. With zero occupied Allocations/open Jobs, an empty d2core list and the node drained, the tool inserted only `GameInfo/GMS/Advertise 1` into `gameinfo.gi` and retained its SHA-named original backup. The backup SHA256 matched the pre-edit digest `1fa387b7d02e56bb83f8daab572dae2d5511b173f99f1360ee3068fa789efb25`; the modified file digest was `f3a2bcf174720d81a9d96c97e66f24141a28a2994e3da41ccbc9967631181f69`. No firewall, NAT, VPK or d2core protocol setting was changed.

Fresh instances of the existing verified `2307479570/custom` template were created **one at a time** on explicit ports 28000, 28001 and 28002. Each reached full d2core Ready. A2S_INFO returned on loopback and the public IP, including the public challenge/response path; Controller independently reported `a2s_query_ok=true` while a Ready instance existed. The owner then launched the Steam URI from a browser and confirmed actual Dota entry on all three ports. The first pre-correction attempt had reported an AppID error with no Dota launch; its disappearance after the A2S correction is a correlation, not proof that this setting was the only cause, because the client launch conditions also differed. Each owner-completed instance was stopped and reached `reclaimed/stopped/complete` before the next was created.

An additional temporary instance on port 28003 reached Ready and passed loopback/public A2S_INFO. The owner elected to stop the full-port trial before testing that URI. Port 28003 was fully reclaimed, the direct d2core list was empty, explicit Controller reconciliation completed generation 12/12, and the Linux node resumed. With no Ready instance, Controller's current `a2s_query_ok=false` is expected; the enabled diagnostic config remains true. The owner preferred not to test all ten ports now. Per the frozen node-level rule, this is **partial evidence only**: Linux Steam `verified/enabled` remain false, Steam one-click buttons stay hidden, and `connect` remains the player fallback. Steamchina and Windows URI trials were not performed. The authorized Linux development A2S file change and its original backup remain in place for future trials.
