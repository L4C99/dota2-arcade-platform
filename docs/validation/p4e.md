# P4E restart, reconcile, and real fault drill

P4 start: `c233de400d9833a198624152c977b9bb3e810927`. P4A–P4D checkpoint and follow-up commits are recorded in their validation files. P4E used fixed d2core v0.1.1 (`988720ad85af1f0d97bfe98ec4da4fcbb070beea`) on the existing development Linux and Windows Nodes. No production service, firewall, NAT, content, VPK, or d2core release was changed.

## IMPLEMENTED

- The administrator may now issue an audited, ordinary stop Job for a trusted old instance after its quarantined Request was abandoned. The old Request remains `abandoned` and capacity remains occupied until d2core proves `reclaimed/stopped/complete`. A duplicate open stop Job and an already reclaimed resource are rejected. This closes a recovery gap found during P4E. The admin instance view exposes this action for an abandoned Request only while its old Allocation is quarantined.
- Narrow mobile admin navigation displays all five sections on two rows. It retains the accepted ivory, ink-green, sage, rounded-card visual language.
- `docs/admin-api.md` documents the post-abandon stop rule. No d2core instance restart is used by any next-game flow.

## AUTOMATED VERIFIED

- `gofmt`, `go test ./... -count=1`, and `go vet ./...`: PASS locally. Web lint, typecheck, five Vitest cases, and production build: PASS.
- Full Store and HTTP API suites: PASS against disposable schemas on the authorized development PostgreSQL host after the new abandoned-quarantine regression was added. The regression proves audit-backed stop creation, unchanged abandoned/quarantined occupied state while stop is accepted, full triple-confirmed reclaim, and rejection of duplicate/terminal stop. Existing migration-upgrade, unknown/no-effect, capacity, owner/Party, AdminSession, and reconcile-order suites also passed. The first full run caught an expected-error mismatch in the new test; it was corrected and the targeted and full suites were rerun to PASS.
- Edge browser fixture checks: player `unknown`, `quarantined`, abandon confirmation, `running`, `stopping`, paused next-game and reclaimed states; admin login and all five tabs; abandoned-quarantine admin stop action. Desktop 1440 px, mobile 390 px and narrow mobile 320 px passed with no page-level horizontal overflow. Screenshots remain in ignored `.local/`, not Git.
- Runner tests cover d2core list → open/unknown Job reconciliation → active instance fact reconciliation → new Job claim. A deliberately lost real create/stop response was not injected into a running Dota process during this substage; P4A's disposable PostgreSQL and Controller fault matrix supplies that specific evidence.

## REAL-ENVIRONMENT VERIFIED

- Preflight found no occupied Allocation, open NodeJob, d2core instance, or Dota process. A formal development PostgreSQL backup was made before upgrade (SHA-256 `fae34eda2a90bac8defbb446d1eeb50fa688d42148c8f18967dae1b301d64893`). The existing business database upgraded from migration 10 to migration 13 without deletion or rewrite. The development Platform and Web were switched to P4 builds using a preserved launcher backup; the fixed-source P4D Controllers reported compatible fresh heartbeats on both OSes. The later P4E recovery candidate was deployed to a new binary/Web directory with another preserved launcher backup. HTTPS health, player/admin pages and hashed assets returned 200.
- One Ready Allocation on each Node survived a Platform restart with the same Allocation, frozen create Job, instance ID, d2core status and actual port (Linux 28000, Windows 28100). Both Controller restarts and Linux d2core manager restart likewise preserved the same resource identity, JoinInfo port and single core instance. Formal stop and full reclaim succeeded.
- The first Windows manager restart attempt exposed a development test-session lifetime issue: a short-lived SSH-launched manager also ended its Dota child when that SSH session closed. This was **not** counted as recovery success. The failed instance was formally stopped and fully reclaimed. A repeat test kept the original ordinary-account SSH job alive while restarting the fixed manager in a separate foreground session. The original Windows Dota PID 8132 remained running and d2core recovered the same Ready instance at port 28100. Controller reconnection preserved the same Allocation/Job/instance; formal stop reached full reclaim. A second fresh Windows request then reached Ready and full reclaim. No Windows service or persistent scheduled task was added.
- A Linux next-game intent was recorded while its Controller was stopped, remained `pending` across a Platform restart, and was consumed once after Controller reconnect and old full reclaim. The new manual request inherited the chosen Node and had a fresh `requested_at`, a distinct Allocation/Job/instance and normal FIFO allocation; the new instance reached Ready and full reclaim.
- Both Controllers were then stopped for a sustained heartbeat outage with one Ready instance on each Node. At approximately 132 seconds of heartbeat age both were `stale`; at approximately 311 seconds both were `offline`. Player Node choices reported `unreachable`; each old Node remained occupied `1/1`, the same Allocation and frozen create Job remained running, and direct d2core status/list showed the same single Ready instance and port. Neither Node received a duplicate create or released old capacity. Reconnection restored fresh compatible heartbeats and the same IDs/JoinInfo before formal stop; both instances reached `reclaimed/stopped/complete`. This closes P3's carried **dual-node real sustained stale/offline drill** with new P4 evidence, without rewriting P3 acceptance.
- The frozen quarantine default was read as 15 minutes in the migrated development database. A real Linux instance was manually quarantined while a next-game stop Job was pending and the Controller was stopped. The next-game intent became `paused`; Platform restart retained the quarantine, occupied capacity, paused intent, AdminSession and Audit. The player twice confirmed abandon: exactly one new manual Request was created, remained waiting while the old core instance still ran and occupied capacity, and did not claim the old server stopped. Reconnected Controller completed the old stop/full reclaim, then the new request reached Ready and full reclaim.
- A second real Linux instance was quarantined and abandoned without a pending stop. A fresh manual request remained waiting on the occupied Node. The new admin `request.stop` action created the trusted old-instance stop Job; only full d2core reclaim freed the old capacity, after which the new request reached Ready and full reclaim. A one-use test AdminUser password was supplied through stdin, never printed or committed. Its AdminSession and Audit survived Platform restart. The account was disabled after the drill and its existing session returned 401. No development entry was marked verified/enabled.

## FINAL DEVELOPMENT STATE AT CHECKPOINT

Read-only database and direct Node checks after the real drills: migration 13; 41 `reclaimed` Allocations, zero occupied, failed-unreclaimed or quarantined; 39 ended, two abandoned and three cancelled historical Requests; no open/unknown NodeJob; two consumed next-game intents and none pending/paused. The one-use AdminUser is disabled, its session is unusable, and Audit includes admin create/disable, two quarantine actions and an admin stop. Both Nodes are compatible/online with fresh heartbeats, Drain false, priority 0, enabled/accepting true, desired/hard/occupied `1/1/0`, and Controller-reported `p1-test-v1 / confirmed` content. Steam and steamchina verified/enabled remain false. Platform accepting is true and the quarantine setting remains the frozen `15 minutes`. Direct fixed d2core `list --json` returned empty on both Nodes; managers and Controllers were running, and no Dota process was observed. Historical ended/reclaimed/Audit records were retained.

## NOT VERIFIED

- Deliberately forcing a real create/stop response loss at the exact d2core call boundary was not repeated in P4E; P4A disposable fault tests and the P4E real restart/outage checks cover the corresponding durable invariants without disrupting a real Dota process at that timing boundary.
- The automatic 15-minute quarantine transition was integration-tested and the real deployment default was confirmed. The real quarantine/escape drill used the authorized early manual quarantine path, so it is not evidence of waiting 15 minutes for automatic isolation.
- Windows VM public human join, a second public topology, vendor NAT, asymmetric NAT, and P5 A2S/Steam/steamchina verified workflow remain NOT VERIFIED. Final project-owner player/admin UI acceptance and P4 closure are later steps, not claimed here.
