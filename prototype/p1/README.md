# P-1 已验收 UI / UX 原型

**P-1 UI / UX 已由项目所有者验收通过。**本目录保留最终确认的视觉方向与交互方案，供后续正式前端参考；它不是生产前端，不连接 API、节点或 d2core。所有数据和状态变化均为本地模拟。

正式实现必须继续遵守 [冻结 V1 规格](../../docs/specs/v1.md)。**不得从原型的 mock 数据、字段展示、按钮模拟或示例编号推导正式 API、数据库 schema 或部署配置。**具体接口和数据路径应在相应开发阶段依照冻结规格设计。

最终方向为舒展卡片式玩家页面，配套正常申请流程、队伍与异常页面，以及管理员后台原型。玩家首页的“全部地图”“游戏模式”分别对应规格中的 `ArcadeGame` 和 `GamePreset`，不代表新增实体。

| 最终参考页面 | 范围 |
| --- | --- |
| [index.html](index.html) | 玩家首页的视觉方向、地图和玩法选择、申请摘要及已有申请预览 |
| [flow.html](flow.html) | 正常申请、等待、启动、进房、结束和下一局 |
| [party.html](party.html) | 队伍、邀请、状态未知与异常隔离 |
| [admin.html](admin.html) | 管理员登录、概览与公告、内容与版本、节点与入口、申请与实例 |

## 本地查看

直接用 Chrome 或 Edge 打开上述页面；也可在本目录运行 `python -m http.server 8000`，再访问 `http://localhost:8000/`。无需安装依赖。页面顶部的演示控件用于切换原型状态，不是正式产品入口。

玩家首页中的游戏、节点、容量和延迟均为虚构示例；没有真实 IP、域名或部署参数。全局维护提示、内容维护提示、节点可用状态与活动申请状态均为视觉示例。

## P-1B 玩家正常流程

打开 [flow.html](flow.html) 查看正常流程原型。选择地图、游戏模式及自动/手动节点后提交申请；顶部演示栏可依次模拟节点受理、服务器就绪及完整回收。页面展示等待、启动、Ready、手动 `connect`、一键进入、结束和下一局。纯 waiting 且尚未分配时可演示安全取消。

一键入口、节点状态、连接地址与复制内容均为模拟示例，不会启动 Steam 或连接服务器。原型以“已验证并启用的 Steam 入口”和“未验证的蒸汽平台入口”展示开放条件；正式产品必须按冻结规格检查真实 verified/enabled 及 JoinInfo。下一局沿用当前地图、模式和节点方式，先完整回收、再创建新申请并重新排队，不调用实例 restart。

手动进房帮助参考项目所有者提供的现用平台说明，使用 Steam 中 Dota 2 的启动选项 `-console`，不包含与控制台无关的 `-perfectworld`。具体设置路径和控制台按键尚未在真实 Dota 2 客户端独立复核，不作为最终帮助文案。此页没有 API、持久化、真实轮询或 d2core 集成。

## P-1C 队伍与异常

打开 [party.html](party.html) 查看队伍、邀请及异常状态原型。顶部演示栏可切换未入队、收到邀请、队长/队员、状态未知、异常隔离和“下一局”回收异常场景。演示队长创建/解散队伍、复制/重置邀请链接、队员加入/退出，以及队长在异常隔离后经二次确认“放弃此异常服务器并继续”。

状态未知时不提供放弃入口，也不重新创建或改派；异常隔离后旧申请与资源记录保留，旧节点容量继续占用。普通放弃只解除阻塞，随后由队长重新选择并申请；若下一局意图已暂停，放弃后才创建新的等待申请。所有队伍、邀请地址和异常状态均为模拟数据，不连接 API，也不影响真实资源。展示的 `example.invalid` 链接不是正式邀请凭证；正式产品必须使用不可猜测的高熵邀请链接。

## P-1D 管理员原型

打开 [admin.html](admin.html) 查看管理员登录及管理后台原型。登录仅检查输入是否填写，不发送或保存凭据。后台按概览、内容、节点、实例分区，展示全局维护与公告、ArcadeGame/GamePreset 控制、ContentVersion 发布、节点容量/Drain、Node × ArcadeGame 接受新分配、一键入口 verified/enabled，以及 waiting、运行中、quarantined 实例的操作边界。

顶部演示栏可模拟 Controller 上报内容版本、网络配置变更，以及节点恢复后 d2core 完整回收；这些不是管理员直接改写节点事实的产品操作。发布版本需目标节点上报匹配版本，入口未验证不能开放，stop/resync 不直接释放容量。所有状态只保存在当前页面内，不能用于真实管理；真实登录安全、审计、权限、轮询、API 和后端校验留待后续实现。

“内容与版本”展示两张模拟地图的独立设置；“节点与入口”中的青岚一号也可分别查看、暂停或恢复这两张地图在该节点上的新分配。节点上报的地图版本为只读演示数据，不会因切换或修改分配开关而改变。

星潮远征的版本卡同时列出三台节点：青岚一号可模拟上报并验证 v3，松影二号保留上次确认的 v2，溪谷四号离线、版本无法确认。启用 v3 只改变平台后续分配的目标版本；其他节点不会因此自动升级，已分配服务器仍保留原版本。

一键入口实测通过后，确认按钮显示已完成并停用；模拟网络配置变化会使 Steam 和蒸汽平台的实测及开放状态同时失效，按钮恢复待确认。原型不向管理员展示网络配置的内部修订号；真实平台仍需按冻结规格记录配置修订或指纹，确保配置变化时旧验证立即失效。
